server:
  port: 7070
spring:
  application:
    name: jach-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Anderson897lk/24JuevesExamen.git

          searchPaths: config-data
          default-label: main

  security:
    user:
      name: root
      password: 123456